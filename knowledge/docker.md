# Docker

## Definition
Docker is a tool that packages an application and everything it needs to run into a container.

## Analogy
Think of Docker like a lunchbox for your application. Instead of carrying food, it carries your code, dependencies, environment settings, and runtime.

## Common Commands

### Pull an image
```bash
docker pull nginx